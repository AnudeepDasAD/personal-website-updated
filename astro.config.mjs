// @ts-check
import { defineConfig } from 'astro/config';

// Set `site` to your final URL before deploying (e.g. GitHub Pages or UWaterloo hosting)
export default defineConfig({
  site: 'https://example.com',
});
